import { Navigation } from '../Navigation/Navigation';

import './Footer.scss';

export const Footer = () => {
  return (
    <footer className="footer">
      <div className="footer__wrapper">
        <Navigation />
      </div>
    </footer>
  );
}